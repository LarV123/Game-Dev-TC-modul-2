#include <stdio.h>

void Move(int x, int y, int deltaX, int deltaY){
    x+=deltaX;
    y+=deltaY;
}

int main(){
    int playerX, playerY;
    int playerHealth = 0;
    int enemyX, enemyY;
    int enemyHealth = 0;

}